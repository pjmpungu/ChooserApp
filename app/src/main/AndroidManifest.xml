<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jonathannalikka.chooserapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChooserApp">
        <activity android:name=".ChosenPersonActivity"
            android:theme="@style/AppTheme.AppDialogTheme"></activity>
        <activity android:name=".ChooserActivity" />
        <activity
            android:name=".NewPersonActivity"
            android:theme="@style/AppTheme.AppDialogTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>